{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "yarn workspace @notedown/app-desktop build",
    "pack": "yarn workspace @notedown/app-desktop pack",
    "dist": "yarn workspace @notedown/app-desktop dist",
    "release": "yarn workspace @notedown/app-desktop release"
  },
  "devDependencies": {},
  "workspaces": [
    "packages/*"
  ]
}
